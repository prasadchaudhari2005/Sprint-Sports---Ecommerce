<!DOCTYPE html>
<html lang="en">

<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>User Login - Sprint Sport</title>
  <!-- Bootstrap CSS -->
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
  <!-- Font Awesome -->
  <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css">
  <!-- Main CSS -->
  <link rel="stylesheet" href="/style.css">
</head>

<body>
  <div class="login-wrapper">
    <div class="login-container">
      <h1><i class="fas fa-user-circle"></i> Login</h1>

      <% if (error_msg && error_msg.length> 0) { %>
        <div class="error-msg">
          <i class="fas fa-exclamation-circle"></i>
          <%= error_msg %>
        </div>
        <% } %>

          <form action="/user_login" method="POST">
            <label>Email Address</label>
            <div class="input-group mb-3">
              <span class="input-group-text bg-dark border-secondary text-muted"><i class="fas fa-envelope"></i></span>
              <input type="email" name="email" class="form-control" placeholder="Enter your email" required>
            </div>

            <label>Password</label>
            <div class="input-group mb-3">
              <span class="input-group-text bg-dark border-secondary text-muted"><i class="fas fa-lock"></i></span>
              <input type="password" name="password" class="form-control" placeholder="Enter your password" required>
            </div>

            <button type="submit" class="mt-3">
              Login <i class="fas fa-arrow-right ms-2"></i>
            </button>
          </form>

          <p class="mt-4 text-muted">Don't have an account? <a href="/register" class="fw-bold text-primary">Register
              Now</a></p>
          <div class="mt-2">
            <a href="/" class="text-muted" style="font-size: 0.9rem;"><i class="fas fa-home"></i> Return to Home</a>
          </div>
    </div>
  </div>
</body>

</html>