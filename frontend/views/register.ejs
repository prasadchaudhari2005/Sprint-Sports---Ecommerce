<!DOCTYPE html>
<html lang="en">

<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Register - Sprint Sport</title>
  <!-- Bootstrap CSS -->
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
  <!-- Font Awesome -->
  <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css">
  <!-- Main CSS -->
  <link rel="stylesheet" href="/style.css">
</head>

<body>
  <div class="login-wrapper">
    <div class="signup-container">
      <h1><i class="fas fa-user-plus"></i> Join The Team</h1>

      <form action="/register" method="POST">
        <div class="row text-start">
          <div class="col-md-12">
            <label>Full Name</label>
            <input type="text" name="name" class="form-control" placeholder="John Doe" required>
          </div>
        </div>

        <div class="row text-start">
          <div class="col-md-6">
            <label>Age</label>
            <input type="number" name="age" class="form-control" min="1" required>
          </div>
          <div class="col-md-6">
            <label>Mobile No</label>
            <input type="tel" name="phone_number" class="form-control" pattern="[0-9]{10}"
              title="Enter a 10-digit number" required>
          </div>
        </div>

        <div class="row text-start">
          <div class="col-md-12">
            <label>Address</label>
            <input type="text" name="address" class="form-control" required>
          </div>
        </div>

        <div class="row text-start">
          <div class="col-md-12">
            <label>Email</label>
            <input type="email" name="email" class="form-control" required>
          </div>
        </div>

        <div class="row text-start">
          <div class="col-md-12">
            <label>Password</label>
            <input type="password" name="password" class="form-control" required>
          </div>
        </div>

        <button id="registered" type="submit" class="mt-2">
          Create Account <i class="fas fa-check-circle ms-2"></i>
        </button>
      </form>

      <p class="mt-4 text-muted">Already a member? <a href="/user_login" class="fw-bold text-primary">Login Here</a></p>
    </div>
  </div>

  <script>
    document.getElementById('registered').addEventListener('click', (e) => {
      // Simple check to ensure form is valid before alerting (optional, but good UX)
      const inputs = document.querySelectorAll('input');
      let valid = true;
      inputs.forEach(i => { if (!i.checkValidity()) valid = false; });

      if (valid) {
        // Retaining the user's requested alert logic
        // Note: in a real app, you might check if the submission was actually successful first, 
        // but user had a click listener. We will keep it but only if valid.
        // Ideally this should be a submit event on the form, but sticking to user's implementation structure.
        // However, form submission refreshes page, so alert might be cut off. 
        // Moving this to form 'submit' listener or just keeping 'click' as requested.
        alert('You are registered successfully!');
      }
    });
  </script>
</body>

</html>