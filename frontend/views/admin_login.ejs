<!DOCTYPE html>
<html lang="en">

<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Admin Login - Sprint Sport</title>
  <!-- Bootstrap CSS -->
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
  <!-- Font Awesome -->
  <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css">
  <!-- Main CSS -->
  <link rel="stylesheet" href="/style.css">
  <style>
    .login-container h1 {
      color: var(--secondary);
      /* Electric Blue for Admin */
    }

    .login-container button {
      background: var(--secondary);
      border: 1px solid var(--secondary);
      color: black;
    }

    .login-container button:hover {
      background: #00b8e6;
      box-shadow: 0 0 15px rgba(0, 204, 255, 0.4);
    }
  </style>
</head>

<body>
  <div class="login-wrapper">
    <div class="login-container">
      <h1 class="mb-4"><i class="fas fa-user-shield"></i> Admin Portal</h1>

      <% if (error_msg && error_msg.length> 0) { %>
        <div class="error-msg">
          <i class="fas fa-exclamation-triangle"></i>
          <%= error_msg %>
        </div>
        <% } %>

          <form action="/admin_login" method="POST">
            <label>Admin Email</label>
            <div class="input-group mb-3">
              <span class="input-group-text bg-dark border-secondary text-muted"><i class="fas fa-envelope"></i></span>
              <input type="email" name="email" class="form-control" placeholder="admin@example.com" required>
            </div>

            <label>Password</label>
            <div class="input-group mb-3">
              <span class="input-group-text bg-dark border-secondary text-muted"><i class="fas fa-key"></i></span>
              <input type="password" name="password" class="form-control" placeholder="Enter credential" required>
            </div>

            <button type="submit" class="mt-3">
              Verify Access <i class="fas fa-unlock ms-2"></i>
            </button>
          </form>

          <p class="mt-4 text-muted">Need access? <a href="/admin_signup" style="color: var(--secondary);">Request
              Here</a></p>
          <div class="mt-2">
            <a href="/" class="text-muted" style="font-size: 0.9rem;"><i class="fas fa-home"></i> Return to Home</a>
          </div>
    </div>
  </div>
</body>

</html>