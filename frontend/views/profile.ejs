<!DOCTYPE html>
<html lang="en">

<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>User Profile - Sprint Sport</title>
  <!-- Bootstrap CSS -->
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
  <!-- Font Awesome -->
  <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css">
  <!-- Main CSS -->
  <link rel="stylesheet" href="/style.css">
</head>

<body>

  <%- include('partials/header') %>

    <div class="container mt-5 mb-5" style="max-width: 600px;">
      <div class="login-container">
        <h1 class="text-center mb-4"><i class="fas fa-id-card"></i> User Profile</h1>

        <div class="text-center mb-4">
          <div
            style="width: 80px; height: 80px; background: var(--primary); color: #000; font-size: 40px; border-radius: 50%; display: flex; align-items: center; justify-content: center; margin: 0 auto; font-weight: bold;">
            <%= user.name.charAt(0).toUpperCase() %>
          </div>
          <h4 class="mt-2 text-white">
            <%= user.name %>
          </h4>
          <p class="text-muted">Member since <%= new Date().getFullYear() %>
          </p>
        </div>

        <ul class="list-group list-group-flush bg-transparent">
          <li class="list-group-item bg-transparent text-white border-secondary d-flex justify-content-between">
            <span class="text-muted">Email</span>
            <span>
              <%= user.email %>
            </span>
          </li>
          <li class="list-group-item bg-transparent text-white border-secondary d-flex justify-content-between">
            <span class="text-muted">Phone</span>
            <span>
              <%= user.phone_number %>
            </span>
          </li>
          <li class="list-group-item bg-transparent text-white border-secondary d-flex justify-content-between">
            <span class="text-muted">Age</span>
            <span>
              <%= user.age %>
            </span>
          </li>
          <li class="list-group-item bg-transparent text-white border-secondary d-flex justify-content-between">
            <span class="text-muted">User ID</span>
            <span class="font-monospace text-primary">
              <%= user.id %>
            </span>
          </li>
        </ul>

        <div class="mt-4 text-center">
          <a href="/my-bills" class="neon-btn w-100 mb-2">My Orders</a>
          <a href="/logout" class="btn btn-outline-danger w-100">Logout</a>
        </div>
      </div>
    </div>

    <%- include('partials/footer') %>

      <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
</body>

</html>