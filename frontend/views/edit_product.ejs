<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Edit Product - Sprint Sport</title>
    <!-- Bootstrap CSS -->
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
    <!-- Font Awesome -->
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css">
    <!-- Main CSS -->
    <link rel="stylesheet" href="/style.css">
</head>

<body>
    <%- include('partials/admin-header') %>

        <div class="container mt-5 mb-5" style="max-width: 500px;">
            <div class="login-wrapper" style="min-height: auto; padding: 0;">
                <div class="signup-container w-100">
                    <div class="mb-4 text-start">
                        <a href="/admin/manage-products" class="text-white text-decoration-none">
                            <i class="fas fa-arrow-left me-2"></i> Back
                        </a>
                    </div>
                    <h1 class="mb-2 text-center text-white">Update Product</h1>
                    <h4 class="text-center text-primary mb-4">
                        <%= product.product_name %>
                    </h4>

                    <form action="/admin/update-product/<%= product.product_id %>" method="POST"
                        enctype="multipart/form-data">
                        <!-- Price -->
                        <div class="mb-3 text-start">
                            <label class="form-label text-white fw-bold">Price (₹)</label>
                            <input type="number" name="price" class="form-control"
                                value="<%= Number(product.price).toFixed(2) %>" step="0.01" min="0" required>
                        </div>

                        <!-- Description -->
                        <div class="mb-3 text-start">
                            <label class="form-label text-white fw-bold">Description</label>
                            <textarea name="description" class="form-control" rows="4"
                                required><%= product.description %></textarea>
                        </div>

                        <!-- Image URL -->
                        <div class="mb-3 text-start">
                            <label class="form-label text-white fw-bold">Image URL</label>
                            <input type="text" name="img_url" class="form-control" value="<%= product.img_url %>"
                                placeholder="Enter image URL">
                        </div>

                        <!-- OR File Upload -->
                        <div class="mb-4 text-start">
                            <label class="form-label text-white fw-bold">Or Upload Image</label>
                            <input type="file" name="image" class="form-control" accept="image/*">
                            <small class="text-muted">Uploading a file will override the URL.</small>
                        </div>

                        <button type="submit" class="neon-btn w-100">
                            <i class="fas fa-sync-alt me-2"></i> Update Product
                        </button>
                    </form>


                </div>
            </div>
        </div>

        <%- include('partials/footer') %>
            <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
</body>

</html>