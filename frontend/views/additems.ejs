<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Add Product - Sprint Sport</title>
    <!-- Bootstrap CSS -->
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
    <!-- Font Awesome -->
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css">
    <!-- Main CSS -->
    <link rel="stylesheet" href="/style.css">
</head>

<body>
    <%- include('partials/admin-header') %>

        <div class="container mt-5 mb-5" style="max-width: 600px;">
            <div class="login-wrapper" style="min-height: auto; padding: 0;">
                <div class="signup-container w-100">
                    <div class="mb-4 text-start">
                        <a href="/admin" class="text-white text-decoration-none"><i class="fas fa-arrow-left me-2"></i>
                            Back</a>
                    </div>
                    <h1 class="mb-4 text-center"><i class="fas fa-plus-square text-success"></i> Add New Product</h1>

                    <form action="/additems" method="POST" enctype="multipart/form-data">
                        <div class="mb-3 text-start">
                            <label class="form-label text-white fw-bold">Product Name</label>
                            <input type="text" name="name" class="form-control" placeholder="e.g., Cricket Bat"
                                required>
                        </div>

                        <div class="row text-start">
                            <div class="col-md-6 mb-3">
                                <label class="form-label text-white fw-bold">Price (₹)</label>
                                <input type="number" name="price" class="form-control" step="0.01" min="0"
                                    placeholder="e.g., 1500.00" required>
                            </div>
                            <div class="col-md-6 mb-3">
                                <label class="form-label text-white fw-bold">Stock Quantity</label>
                                <input type="number" name="quantity" class="form-control" min="0" placeholder="e.g., 15"
                                    required>
                            </div>
                        </div>

                        <div class="mb-3 text-start">
                            <label class="form-label text-white fw-bold">Image URL</label>
                            <input type="url" name="img_url" class="form-control"
                                placeholder="https://example.com/image.jpg">
                        </div>

                        <div class="mb-3 text-start">
                            <label class="form-label text-white fw-bold">Or Upload Image</label>
                            <input type="file" name="image" class="form-control" accept="image/*">
                            <small class="text-muted">Uploading a file will override the URL.</small>
                        </div>

                        <div class="mb-4 text-start">
                            <label class="form-label text-white fw-bold">Description</label>
                            <textarea name="description" class="form-control" rows="3"
                                placeholder="Enter product description" required></textarea>
                        </div>

                        <button type="submit" class="neon-btn w-100">
                            <i class="fas fa-save me-2"></i> Save Product
                        </button>
                    </form>


                </div>
            </div>
        </div>

        <%- include('partials/footer') %>
            <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
</body>

</html>